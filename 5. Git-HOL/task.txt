Exercise 5: Finalizing and Pushing Changes to Remote Repository

Objective:
Confirm a clean working state on the main branch, examine all branches, fetch updates from the remote repository, and upload any pending commits.

Steps:
git status
git branch -a
git pull origin master
git push origin master